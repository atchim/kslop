# liquorix — knobs unique to Damentz's patched mainline kernel.
#
# Draft — first build not yet attempted. Populate the Verified section
# after the first successful merge+build.

CONFIG_SCHED_BORE=y
CONFIG_HZ_1000=y
CONFIG_PREEMPT=y

# vim: ft=conf
